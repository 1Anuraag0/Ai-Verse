/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // ... existing config options ...
  // swcMinify: true, // This line is causing the warning
  // ... existing config options ...
}

module.exports = nextConfig 